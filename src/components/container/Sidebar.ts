import { createComponent, createStaticComponent } from "../../utils/component"

export const Sidebar = createComponent(() => {
  return SidebarWrap([
    // todo
  ])
})

const SidebarWrap = createStaticComponent("div")
