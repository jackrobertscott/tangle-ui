import { createComponent, createCssComponent } from "../../utils/component"

export const Sidebar = createComponent(() => {
  return SidebarWrap([
    // todo
  ])
})

const SidebarWrap = createCssComponent("div")
