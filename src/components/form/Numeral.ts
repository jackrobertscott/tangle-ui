import { createComponent, createCssComponent } from "../../utils/component"

export const Numeral = createComponent(() => {
  return NumeralWrap([
    // todo
  ])
})

const NumeralWrap = createCssComponent("div")
