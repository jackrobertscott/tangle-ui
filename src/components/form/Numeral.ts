import { createComponent, createStaticComponent } from "../../utils/component"

export const Numeral = createComponent(() => {
  return NumeralWrap([
    // todo
  ])
})

const NumeralWrap = createStaticComponent("div")
