import { createComponent, createStaticComponent } from "../../utils/component"

export const Poster = createComponent(() => {
  return PosterWrap([
    // todo
  ])
})

const PosterWrap = createStaticComponent("div")
