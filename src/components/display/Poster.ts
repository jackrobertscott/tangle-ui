import { createComponent, createCssComponent } from "../../utils/component"

export const Poster = createComponent(() => {
  return PosterWrap([
    // todo
  ])
})

const PosterWrap = createCssComponent("div")
